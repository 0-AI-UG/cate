{
  "name": "cate-pet-tools",
  "description": "Canvas Pet tool surface — terminal/worktree/todo orchestration tools the headless pet sessions call. Registers nothing unless CATE_PET_ROLE is set, so normal agent sessions are unaffected.",
  "private": true,
  "pi": {
    "extensions": ["./index.ts"]
  }
}
