// Renderer compatibility import. The implementation is shared so the main
// process can normalize errors before placing them in user-facing IPC state.
export { errorMessage } from '../../shared/errorMessage'
